apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    app: myapp-rc
    type: front-end
spec:
  template:
    metadata:
      name: myapp-pod
      labels:
        app: myapp
    spec:
      containers:
        - name: nginx-container
          image: nginx
  replicas: 3
